{
  "name": "playarena",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "@nx/react": "^22.6.5",
    "@nx/vite": "^22.6.5",
    "nx": "^17.2.0",
    "typescript": "^5.5.4"
  },
  "scripts": {
    "nx": "nx",
    "start": "npm --prefix apps/memory-game run dev",
    "build": "npm --prefix apps/memory-game run build",
    "test": "echo \"No tests configured. Run package scripts in apps/memory-game if needed.\"",
    "lint": "echo \"No root lint configured. Use npm --prefix apps/memory-game run lint.\"",
    "format": "echo \"No root format command configured. Use package scripts in apps/memory-game.\""
  },
  "nx": {
    "plugins": []
  }
}
